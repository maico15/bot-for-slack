# Slack app credentials
SLACK_SIGNING_SECRET=your_signing_secret_here
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Bot configuration
CHANNEL_ALLOWLIST=C0123456789,C9876543210     # comma-separated; empty = bot disabled (logs warning)
ESCALATION_USER_ID=U0123456789               # Slack user ID to receive escalation DMs
ESCALATION_KEYWORDS=outage,sev1,no inbound   # optional — two formats:
                                             #   comma-separated terms  →  each term auto-escaped + \b-wrapped
                                             #   raw regex              →  start value with /  e.g. /sev\d|outage/i
                                             # omit entirely to use the built-in default regex

# Server
PORT=3000
